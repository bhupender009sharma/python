colletcs links and data from a site using python module "beautifulSoup4" .

-> it uses collecting data by using html data types from the site

->no. pages that you want to crawl can be changed manually, by
  changing the variable "pages" from the "web_crawler.py" file

-> minus points->
		->site links to be added manually

		->only works on those sites, from which data
		  types of html can be easily accumulated	

		-> does not work on the encrypted sites